LAB-1
Вариант №1.
"Используя класс Process (System.Diagnostics) и его свойство StartInfo, откройте для редактирования документ Microsoft Word и дождитесь окончания его редактирования."

LAB-4
Вариант №5.
"Напишите программу, которая создаёт два потока, а затем дожидается их завершения. Первый поток строит таблицу из n1 значений функции sin(x), начиная с точки x1 с шагом s1 с задержкой каждого шага d1. Второй поток проверяет каждые 0,5 секунды размера файла, созданного первым потоком, и в случае его изменения выводит его новый размер на экран. Таблица выводится в файл поток1.dat. Вывод осуществляется по формату 
dd.mm.yyyy hh:nn:ss.zzz «значение x:8.4» «значение y:8.4»
Значения n1, x1, s1 и d1 запрашиваются до создания потоков и передаются им при создании."

LAB-5
Вариант №5.
"Напишите программу, которая создаёт два потока, а затем дожидается их завершения и печатает на экран значение глобальной переменной y. 
Первый делает 100 вычислений y:=sin(x)2+cos(2·x), а второй x:=cos(y). Каждый из потоков перед вычислением ждёт 40 миллисекунд, 
а после вычисления возобновляет работу другого потока и приостанавливает свою. 
Первым вычисление производит второй поток. 
Приостановка и возобновление работы производится средствами класса System.Threading.Thread. Потоки знают друг о друге и значениях x и y через поля класса."

LAB-8
Вариант №1.
"Напишите программу, в которой два потока помещают произвольные числа в конец очереди, а два других потока извлекают числа из очереди и выводят их на экран. 
Очередь реализовать на основе массива."
